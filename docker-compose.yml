services:
  wymakeup-site:
    image: nginx:1.27-alpine
    container_name: wymakeup-site
    ports:
      - "8080:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
      - ./site/nginx.local.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
